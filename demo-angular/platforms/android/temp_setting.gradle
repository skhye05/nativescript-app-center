android {
            defaultConfig {
                applicationId = "org.nativescript.demoangular"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }